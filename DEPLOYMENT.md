# Deployment

Build command: `npm run build`
Output: `dist`

For Vercel, import the repository and keep the Vite defaults.
The Yandex map currently uses an embedded public map. A drawing editor requires a Yandex Maps JavaScript API key and backend storage.
