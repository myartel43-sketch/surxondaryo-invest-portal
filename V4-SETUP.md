# V4 CMS setup

1. Upload all files to GitHub.
2. Wait for Vercel Ready.
3. In Supabase SQL Editor run `supabase/v4-cms.sql` once.
4. Existing tables `news`, `investment_projects`, `site_settings` are preserved.
5. New tables: `staff`, `documents`, `media_items`, `map_objects`.
